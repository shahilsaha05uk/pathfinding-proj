# Copilot Instructions

## Project Guidelines
- User is building a pathfinding dissertation project with algorithms (AStar, Dijkstra, GBFS, JPS) wrapped in an ILS (Iterative Locality Search) layer. Peak memory tracking needs to accumulate across ILS iterations, not reset for each inner algorithm call.